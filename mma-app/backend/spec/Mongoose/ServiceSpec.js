const matchController = require('../../controllers/match.controller');

describe('Controller Tests', () => {
    it ('Finds the correct matches based on page info', () => {
        expect(true).toBe(true);
    });
});